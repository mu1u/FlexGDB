import{_ as e,c as t,o as i,a4 as a}from"./chunks/framework.DRiVJl7O.js";const n="/FlexGDB/assets/filter.z_m5F8Ga.png",r="/FlexGDB/assets/query_status.BloFvXvM.png",o="/FlexGDB/assets/type_query.C6rFvbD5.png",s="/FlexGDB/assets/fuzzy_query.DZOyY5dn.png",c="/FlexGDB/assets/exact_query.CD61TdK_.png",d="/FlexGDB/assets/comb_query.C2coThGV.png",k=JSON.parse('{"title":"Key Filtering and Matching Queries","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"guide/filter/index.md","filePath":"guide/filter/index.md"}'),l={name:"guide/filter/index.md"},h=a('<h1 id="key-filtering-and-matching-queries" tabindex="-1">Key Filtering and Matching Queries <a class="header-anchor" href="#key-filtering-and-matching-queries" aria-label="Permalink to &quot;Key Filtering and Matching Queries&quot;">​</a></h1><p>The key list on the left side of Tiny RDM provides filtering and matching queries. By filtering and querying, you can quickly locate the keys you need, reducing unnecessary scanning.</p><h2 id="filtering" tabindex="-1">Filtering <a class="header-anchor" href="#filtering" aria-label="Permalink to &quot;Filtering&quot;">​</a></h2><p>Enter the keyword or phrase you want to find in the search input, either partially or completely. Once you have entered the text, without any further action, the loaded list will only display the keys containing the specified keyword.</p><p><img src="'+n+'" alt="Filtering Example"></p><h2 id="querying" tabindex="-1">Querying <a class="header-anchor" href="#querying" aria-label="Permalink to &quot;Querying&quot;">​</a></h2><p>In addition to entering the keyword, a query operation requires you to press <code>Enter</code> or click the 🔍 button. After this, Tiny RDM will use the keyword to rescan and match the server data, and display the results after loading is complete. If the number of search results exceeds the single scan limit, you can click <code>Load More Keys</code> or <code>Load All Left Keys</code> to complete the batch loading.</p><p>If you need to modify or replace current query keywords, you can <code>double-click</code> the tag or enter a new keyword and press enter again.</p><p>In query status, the keywords are displayed as tags in the input box: <img src="'+r+'" width="300" alt="Input box in query status"></p><h3 id="type-matching" tabindex="-1">Type Matching <a class="header-anchor" href="#type-matching" aria-label="Permalink to &quot;Type Matching&quot;">​</a></h3><p>By using the type dropdown before the search input box, you can make Tiny RDM only scan keys of the specified type.</p><p><img src="'+o+'" alt="Type Matching Example"></p><h3 id="fuzzy-matching" tabindex="-1">Fuzzy Matching <a class="header-anchor" href="#fuzzy-matching" aria-label="Permalink to &quot;Fuzzy Matching&quot;">​</a></h3><p>By default, Tiny RDM will add the wildcard <code>*</code> as a prefix and suffix to the keywords before scanning. After pressing <code>Enter</code> to confirm the search keyword, the input bar status will be as follows, indicating that the fuzzy search is performed using the regular expression condition <code>*JSON*</code>:</p><img src="'+s+'" width="350" alt="Example of fuzzy matching"><h3 id="exact-matching" tabindex="-1">Exact Matching <a class="header-anchor" href="#exact-matching" aria-label="Permalink to &quot;Exact Matching&quot;">​</a></h3><p>When the exact match switch is on, no wildcards will added by Tiny RDM, it only performs an <code>EXISTS</code> command to exactly match the keywords.</p><img src="'+c+'" width="350" alt="Example of exact matching"><h2 id="combined-use-filtering-querying" tabindex="-1">Combined Use (Filtering + Querying) <a class="header-anchor" href="#combined-use-filtering-querying" aria-label="Permalink to &quot;Combined Use (Filtering + Querying)&quot;">​</a></h2><p>As long as it is properly used, the combination of filter + query can achieve the purpose of secondary filtering keys and speed up reaching the target key:</p><ol><li>Select the type to query (optional)</li><li>Enter the keyword for fuzzy querying, and press <code>Enter</code> to confirm.</li><li>After the scan result set appears, input the keyword to filter again (no need to press <code>Enter</code> this time, pressing <code>Enter</code> again can replace the fuzzy query keyword in step 2)</li></ol><img src="'+d+'" width="350" alt="Example of combined querying">',22),u=[h];function p(y,g,f,m,_,x){return i(),t("div",null,u)}const b=e(l,[["render",p]]);export{k as __pageData,b as default};
